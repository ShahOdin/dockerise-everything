# Dockerise everything!

## Runnning the service

From the root folder, simply: 

```bash
cd run
docker compose up
```

## Backend tests

From the root folder, simply:

```bash
cd be-test
docker compose up
```